//
Procedure run() Экспорт
	Message("run: initialize.bsl");
	Сообщить(""+log_file);
	ПеренестиЛоги();
EndProcedure // run()

Процедура ПеренестиЛоги()
	логи = НайтиФайлы("log\", "*.log");
	Для каждого лг Из логи Цикл
		Сообщить(лг);
	КонецЦикла;
КонецПроцедуры
